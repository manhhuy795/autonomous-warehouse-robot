# Eval dataset placeholder

Thư mục này chỉ nên commit file hướng dẫn và `ground_truth.csv` mẫu. Ảnh dataset đầy đủ, đặc biệt các bộ ảnh chuyển động, không nên đưa vào Git/ZIP source vì làm project nặng.

Để chạy `evaluate_vision_metrics.py`, hãy tự tạo hoặc tải dataset theo `ai-server/DATASET_GUIDE.md`, `ai-server/README_AI_VISION_EVALUATION.md` và đặt ảnh vào các thư mục tương ứng trên máy local.
